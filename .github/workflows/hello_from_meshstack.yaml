name: Hello from meshStack

on:
  workflow_dispatch:
    inputs:
      buildingBlockRun:
        description: "meshStack Building Block Run"
        required: true
     
jobs:
  say-hello:
    runs-on: visa

    steps:
      - name: Say Hello
        run: |
          echo "👋 Hello from meshStack!"
          echo "🏗️ Building Block Run: ${{ inputs.buildingBlockRun }}"
