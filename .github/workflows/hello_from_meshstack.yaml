name: Hello from meshStack

on:
  workflow_dispatch:
    inputs:
      buildingBlockRun:
        description: "meshStack Building Block Run"
        required: true

jobs:
  say-hello:
    runs-on: visa
    steps:
      - name: Print Input
        run:  echo "Building Block Run Input: ${{ github.event.inputs.buildingBlockRun }}"

      - name: Say Hello
        run: echo "👋 Hello from meshStack!"
